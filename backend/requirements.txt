# ===== v2（FastAPI 全功能模式）=====
# 启动： python run.py  （自动检测依赖，缺失时降级为零依赖的 server.py）
fastapi>=0.110
uvicorn>=0.29
numpy>=1.26
pandas>=2.2
APScheduler>=3.10

# MySQL 持久化（旧看板 + 行情库）
PyMySQL>=1.1.0

# ===== 零依赖兜底 =====
# 不安装以上任何包时，run.py 会降级执行 server.py（仅标准库），
# 旧看板功能照常（JSON 缓存模式），新模块（行情/量化/大屏）不可用。
